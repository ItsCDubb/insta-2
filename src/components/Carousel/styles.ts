import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  dotContainer: {
    position: 'absolute',
    flexDirection: 'row',
    justifyContent: 'center',
    bottom: 0,
    width: '100%',
  },
});
